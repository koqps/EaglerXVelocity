# =========================
# CommandToItem by LMBishop
# =========================
#  - Spigot page: https://www.spigotmc.org/resources/19937/
#  - Default config: https://github.com/LMBishop/CommandToItem/blob/master/src/main/resources/config.yml
#  - Source code: https://github.com/LMBishop/CommandToItem
#  - Wiki: https://github.com/LMBishop/CommandToItem/wiki/Configuration

# List items here
items:
  beta:
    # Name & type of item
    name: "&2&lBETA &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the BETA tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.Beta true"
      # Sound to be played
      sound: "ITEM_FLINTANDSTEEL_USE" # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Sound.html
  yas:
    # Name & type of item
    name: "&d&lYASSS &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the YASSS tag"
    on-use:
      commands:
       - "lp user %player% permission set tag.yasss true"
  ironkey:
    # Name & type of item
    name: "&7&lIron &7Key"
    item: TRIPWIRE_HOOK # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to recieve your iron key"
    on-use:
      commands:
       - "crate key %player% iron"
  goldkey:
    # Name & type of item
    name: "&6&lGold &7Key"
    item: TRIPWIRE_HOOK # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to recieve your gold key"
    on-use:
      commands:
       - "crate key %player% gold"
  diamondkey:
    # Name & type of item
    name: "&b&lDiamond &7Key"
    item: TRIPWIRE_HOOK # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to recieve your diamond key"
    on-use:
      commands:
       - "crate key %player% diamond"
  emeraldkey:
    # Name & type of item
    name: "&a&lEmerald &7Key"
    item: TRIPWIRE_HOOK # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to recieve your emerald key"
    on-use:
      commands:
       - "crate key %player% emerald"
  hund:
    # Name & type of item
    name: "&2&l$100"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to recieve $100"
    on-use:
      commands:
       - "eco give %player% 100"
  thousand:
    # Name & type of item
    name: "&2&l$1000"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to recieve $1000"
    on-use:
      commands:
       - "eco give %player% 1000"
  thousand1:
    # Name & type of item
    name: "&2&l$10000"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to recieve $10000"
    on-use:
      commands:
       - "eco give %player% 10000"
  thousand2:
    # Name & type of item
    name: "&2&l$50000"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to recieve $50000"
    on-use:
      commands:
       - "eco give %player% 50000"
  thicc:
    # Name & type of item
    name: "&d&lTHICC &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the THICC tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.thicc true"
  bae:
    # Name & type of item
    name: "&c&lBAE &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the BAE tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.bae true"
  ez:
    # Name & type of item
    name: "&c&lez &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the ez tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.ez true"
  lgbtq:
    # Name & type of item
    name: "&6L&eG&aB&bT&dQ &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the LGBTQ tag"
    on-use:
      commands:
       - "lp user %player% permission set tag.lgtbq true"
  idiot:
    # Name & type of item
    name: "&6&lIDIOT &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the IDIOT tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.idiot true"
  bruh:
    # Name & type of item
    name: "&c&lBRUH &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the BRUH tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.bruh true"
  shishter:
    # Name & type of item
    name: "&dSHISHTER &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the SHISHTER tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.shishter true"
  love:
    # Name & type of item
    name: "&4♥ &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the ♥ tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.love true"
  uwu:
    # Name & type of item
    name: "&bUWU &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the UWU tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.uwu true"
  kawaii:
    # Name & type of item
    name: "&dKAW&5AII &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the KAWAII tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.kawaii true"
  og:
    # Name & type of item
    name: "&cOG &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the OG tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.og true"
  eboy:
    # Name & type of item
    name: "&bEBOY &7tag"
    item: PAPER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Reedeem to access the EBOY tag"
    # Tasks to be done when the item is used
    on-use:
      # Commands to be executed
      commands:
        - "lp user %player% permission set tag.eboy true"
  menu:
    # Name & type of item
    name: "&6Menu"
    item: COMPASS # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/Material.html
    # Lore of item (seen when mouseover)
    lore:
      - "&7Right click to access the menu"
    on-use:
      consume: false
      commands:
       - "zmenu open menu"
    


  msg-console:
    name: "&6&lMessage Console &7(Example Item)"
    item: PAPER
    lore:
      - "&7Message the server"
    on-use:
      consume: false
      commands:
        # You can use "executeas:player" at the beginning of a command to send it as the player
        - "executeas:player msg console Hi"
        - "msg %player% Hi"

  instant-death:
    name: "&c&lInstant Death &7(Example Item)"
    item: SPIDER_EYE
    # You can give an item enchantments
                  # (pre-1.13) USE SPIGOT NAMES -> format "{enchantment}:{level}" -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/enchantments/Enchantment.html
    enchantments: # (1.13+) USE VANILLA NAMES -> namespace for vanilla enchantments is "minecraft" -> format "{namespace}:{enchantment}:{level}"
      - "minecraft:infinity:1"
    # + add the item flag to hide it
    itemflags: # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/inventory/ItemFlag.html
      - "HIDE_ENCHANTS"
    lore:
      - "&7Instantly kills you"
    on-use:
      commands:
        - "kill %player%"
    # & permission required (in this case commandtoitem.use.instant-death)
    options:
      permission-required: true

  strength-sword:
    name: "&e&lInstant Strength &7(Example Item)"
    item: GOLD_SWORD
    lore:
      - "&7Gives you Strength 4"
    on-use:
      # You can add messages which will be sent to the player upon consumption
      # You can use:
      #   %item% for the name of the item
      #   %player% for the players name
      #   %cooldown% for players new cooldown after use
      messages:
        - "&7&oYou feel a sudden rush of blood..."
      commands:
        - "effect %player% 5 15 3"
      # You can add sounds too
      sound: "ENTITY_WITHER_SHOOT"

  infinite-cookie:
    name: "&a&lMagic Cookie &7(Example Item)"
    item: COOKIE
    lore:
      - "&7Does not get consumed"
    on-use:
      # You can prevent items from being taken away when clicked
      consume: false
      # You can also add a short cooldown to prevent it from being spammed
      cooldown: 60
      commands:
        - "heal %player%"

  dodgy-apple:
    name: "&2&lDodgy Apple &7(Example Item)"
    item: APPLE
    # (1.14+ ONLY FEATURE) You can add custom model data
    custommodeldata: 12345
    lore:
      - "&7Something seems off about this apple..."
    on-use:
      commands:
        - "effect give %player% minecraft:poison 15 1"

  speed-boost:
    name: "&7&lSpeed Boost &7(Example Item)"
    item: SUGAR
    # (1.13+ ONLY FEATURE) You can also add attribute modifiers (not recommended if you don't know what you're doing & know how to use it in game)
    attributemodifiers:
      - attribute: GENERIC_MOVEMENT_SPEED # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/attribute/Attribute.html
        modifier:
          uuid: "49dc07dc-bfdb-4dc7-85d3-66ef52b51858" # uuid (/MUST/ BE UNIQUE! - https://www.uuidgenerator.net/)
                                                       # if unspecified, items will not work through restarts as a
                                                       # random one is generated each time the plugin is loaded
          name: "generic.movementSpeed"
          operation: ADD_NUMBER # list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/attribute/AttributeModifier.Operation.html
          amount: 0.03
          equipmentslot: HAND # (optional) list -> https://hub.spigotmc.org/javadocs/spigot/org/bukkit/inventory/EquipmentSlot.html
      - attribute: GENERIC_MOVEMENT_SPEED
        modifier:
          uuid: "e22513cf-b15f-4443-9e2f-103c0ff9731b" # as said before, unique to the previous one
          name: "generic.movementSpeed"
          operation: ADD_NUMBER
          amount: 0.01
          equipmentslot: OFF_HAND
    lore:
      - "&7Hold this item for a minor speed boost,"
      - "&7or consume it for a bigger speed boost."
    on-use:
      commands:
        - "effect give %player% minecraft:speed 30 3"
      cooldown: 30

# Options here
options:
  show-receive-message: true
  drop-if-full-inventory: false # If true and the player has a full inventory, drop the item at their feet. If false and the player has a full inventory, the item is lost.

# Messages here
messages:
  full-inv: "&c%player% doesn't have enough space in their inventory!"
  give-item: "&6Given &e%player% &6%amount% %item%&6."
  receive-item: "&6You have been given %amount% %item%&6."
  receive-item-inventory-full: "&6You have been given %given_amount% %item%&6, but %dropped_amount% dropped at your feet because your inventory is full."
  cooldown: "&cYou must wait &4%cooldown% &cseconds before using this item again."
  no-permission: "&cYou cannot use this item."
  item-list: "&6Items: &e%items%"
  reload: "&7CommandToItem has been reloaded"
  player-not-found: "&cThe specified player could not be found."
  item-not-found: "&cThe item &4%item%&c could not be found."
  item-limits: "&cPlease enter an amount between &4%min%&c and &4%max%&c."